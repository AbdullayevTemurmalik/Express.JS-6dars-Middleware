# 🚀 Backend API (Node.js + Express + MongoDB)

Bu loyiha **Node.js** va **Express.js** asosida yozilgan backend API hisoblanadi.
Ma’lumotlar **MongoDB** bazasiga ulanadi va so‘rovlar **Postman** orqali test qilingan.

## 🧩 Texnologiyalar

* Node.js
* Express.js
* MongoDB (Mongoose)
* Middleware (logger, error handler, body parser)
* Postman (API testing)

## ⚙️ Middleware nima qiladi?

* `express.json()` – request body’ni JSON ko‘rinishida o‘qiydi
* Custom middleware – so‘rovlarni tekshiradi va log qiladi
* Error middleware – xatoliklarni markazlashgan holda ushlaydi

## 📡 API ishlashi

* CRUD amallar (GET, POST, PUT, DELETE)
* RESTful API prinsiplari asosida yozilgan
* MongoDB bilan to‘liq integratsiya qilingan

## ▶️ Ishga tushirish

1. Dependenciyalarni o‘rnating:

   ```bash
   npm install
   ```
2. `.env` fayl yarating va MongoDB URL yozing
3. Serverni ishga tushiring:

   ```bash
   npm run dev
   ```

## 🧪 Test qilish

API endpointlar **Postman** orqali test qilingan.

---
